# Python (for the screener tool, if/when you build it here)
venv/
__pycache__/
*.pyc
results/*.csv
results/*.txt
results/run.log

# OS / editor
.DS_Store
*.swp

# local scan artifacts
fetch_run.log

# Node (not used by the static app, but just in case)
node_modules/

# Console build output
console/dist/
console/public/data

# Engine logs
data/run.log
